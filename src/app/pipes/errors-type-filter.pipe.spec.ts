import { ErrorsTypeFilterPipe } from './errors-type-filter.pipe';

describe('ErrorsTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ErrorsTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
