import { FilterErrorsPipe } from './filter-errors.pipe';

describe('FilterErrorsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterErrorsPipe();
    expect(pipe).toBeTruthy();
  });
});
