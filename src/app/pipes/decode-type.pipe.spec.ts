import { DecodeTypePipe } from './decode-type.pipe';

describe('DecodeTypePipe', () => {
  it('create an instance', () => {
    const pipe = new DecodeTypePipe();
    expect(pipe).toBeTruthy();
  });
});
