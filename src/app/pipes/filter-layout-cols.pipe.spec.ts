import { FilterLayoutColsPipe } from './filter-layout-cols.pipe';

describe('FilterLayoutColsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLayoutColsPipe();
    expect(pipe).toBeTruthy();
  });
});
