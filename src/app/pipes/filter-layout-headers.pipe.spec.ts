import { FilterLayoutHeadersPipe } from './filter-layout-headers.pipe';

describe('FilterLayoutHeadersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLayoutHeadersPipe();
    expect(pipe).toBeTruthy();
  });
});
